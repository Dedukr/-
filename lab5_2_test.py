import numpy as np

# Generate the matrix
n = 7  # Size of the grid
matrix = np.zeros((n, n), dtype=int)

# Fill in the values
for i in range(n):
    for j in range(n):
            matrix[i, j] = 

print(matrix)